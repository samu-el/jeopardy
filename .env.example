# Where the rooms Worker lives. Solo play ignores this; shared rooms need it.
#
# Local (bun run rooms:dev):
NEXT_PUBLIC_ROOMS_URL=http://127.0.0.1:8787
#
# Deployed (after `bun run rooms:deploy`):
# NEXT_PUBLIC_ROOMS_URL=wss://jeopardy-rooms.<your-subdomain>.workers.dev
